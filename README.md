## Subscription CRUDL REST API service

### Summary

This is a test project, consisting of simple REST API implementation. It includes docker-compose.yaml to start this service and swagger.yaml, describing the API.

### Usage

```bash
git clone <repo>
cd <repo directory>
docker-compose up
```
